{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "none" // Prevents default Node.js installation by the feature
    }
  },
  "postCreateCommand": "bash -c \"source $NVM_DIR/nvm.sh && nvm install 20 && nvm alias default 20 && nvm use default\""
}
